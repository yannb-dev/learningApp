"use client";

import { parse, format, differenceInDays } from "date-fns";
import { fr } from "date-fns/locale";

export default function TimeLine({ seances, roadmap }) {
  //   //_______________ Parse de la date createdAt + echeance qui est string _______
  //   const dateEcheance = parse(roadmap.echeance, "dd/MM/yyyy", new Date());
  //   const dateStart = parse(roadmap.createdAt, "dd/MM/yyyy", new Date());

  //   // ______________ Calcule de date entre début de roadmap et échéance ______

  //   console.log(dateStart, dateEcheance);

  return <div></div>;
}
